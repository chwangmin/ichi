# 현재 active 색 upstream (초기값: blue).
# switch.sh 가 upstream.blue.conf / upstream.green.conf 내용으로 이 파일을 덮어쓴 뒤
# edge nginx 를 reload 한다. 직접 편집하지 말 것.
upstream backend_active {
    server ichi-backend-blue:8080;
}
